before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s
  - export PATH="$HOME/.yarn/bin:$PATH"

install:
  - yarn install

script:
  - "yarn run start"

language: "node_js"

node_js: "node"

sudo: false

cache: yarn
